scalar UUID
scalar LocalDateTime

type Favourite{
    id: String!
    userId: UUID!
    restaurantId: UUID!
    createdAt: LocalDateTime
}

type Query {
    favouritesByUserId(userId: ID!): [Favourite!]!
    favourite(id: UUID!): Favourite
}